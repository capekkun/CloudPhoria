---
inclusion: always
---

# Commenting Rules

- Never use decorative header banners or ASCII dividers in comments (e.g., `// =======`, `<!-- ------ -->`, `<%-- ====== -->`).
- Keep section comments short, clean, and lowercase/sentence-case (e.g., `// Exam arena` or `<%-- Exam intro --%>`).
